/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com',"source.unsplash.com","example.com","res.cloudinary.com"],
    },
};

export default nextConfig;
